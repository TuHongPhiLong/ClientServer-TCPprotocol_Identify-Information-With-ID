Version 1.0(End 7 tháng 10): Đả hoàn thành các chức năng:
- Server:
    + Thêm xoá sửa một subject sử dụng mysql.
    + Khởi tạo một server.
    + nhận và gửi dữ liệu(người dùng phải nhấn nút để gửi thông tin subject từ server tới client)

- Client:
    + Kết nối được với server.
    + gửi thông tin và nhận thông tin từ server(đã nhận đúng yêu cầu).

* Mục tiêu Version tới:
    + Xử lý Client gửi id của subject, Server tự động trả về thông tin của subject.
    + Thêm chức năng nhận diện khuôn mặt
    + Thiết kế lại giao diện.
    + Thêm chức năng đăng nhập cho Client.
    + Thêm chức năng thêm lỗi vi phạm cho Client.

Version 1.1(Update ngày 28 tháng 11): Hoàn thiện chức năng gửi và nhận của Server và Client.
- Server: Đã xử lý Client gửi id của subject, Server tự động trả về thông tin của subject(bao gồm cả hình và error của subject).
- Client: Đã xử lý Client gửi id của subject, Server tự động trả về thông tin của subject(bao gồm cả hình và error của subject).

* Mục tiêu Version tới: thêm chức năng đăng nhập cho Client khi kết nối tới Server.

Version 1.2(Update ngày 7 tháng 12): thêm chức năng đăng nhập cho Client khi kết nối tới Server.
- Server: Đã kiểm tra được tài khoản của Client đăng nhập.
- Client:

* Mục tiêu Version tới: Hoàn thiện giao diện.

Version 1.3(): Hoàn thiện giao diện.
-Server:
-Client:

Version 1.5(): thêm chức năng thêm lỗi vi phạm.
ghi chú: Server sau khi gửi idSubject thì sẽ ngừng vòng lập nhận subject để mở vòng lặp nhận error nên không thể nhận idSubject nửa mà chỉ nhận yêu cầu thêm lỗi từ Client
- Server:
- Client:

Version 2.0(): thêm chức năng nhận diện khuôn mặt
-Server:
-Client: